## Nerd Display

A geek information display project running on Linux ARM devices.

## Development

Create virtual environment:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt nerd-display
```
